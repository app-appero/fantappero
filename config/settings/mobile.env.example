# Mobile client (Expo) — copy to apps/mobile/.env
# Only EXPO_PUBLIC_* vars are embedded in the app bundle.
#
# Device fisico: IP LAN del PC (es. http://192.168.1.214:8001), non 127.0.0.1.

EXPO_PUBLIC_API_BASE_URL=http://127.0.0.1:8001
