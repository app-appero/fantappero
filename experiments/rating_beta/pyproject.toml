[project]
name = "fantappero-rating-beta"
version = "0.1.0"
description = "Experimental FantApperò Rating Beta formula (EP00-05)"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
